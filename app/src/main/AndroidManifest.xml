<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oyasis.goldfish">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoldFish">
        <activity android:name=".Achievements"></activity>
        <activity android:name=".HighScores" />
        <activity android:name=".Showcase" />
        <activity android:name=".FirstTime" />
        <activity
            android:name=".GameActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />

        <service
            android:name=".STrackService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".Splash"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>